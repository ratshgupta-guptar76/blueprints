# verification/

Purpose: verification plans, UVM/other testbenches, scripts, and regression summaries.

Structure:
- Keep tests, scoreboards, and verification environment code here. Link to `sim/` testbenches where appropriate.

How to run:
- Prefer `make test` or `scripts/eda.sh test <target>` where available. Include any environment setup required in this folder's README.
